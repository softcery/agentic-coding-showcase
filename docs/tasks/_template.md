# NNN — title

**What.** One line. The change, not the saga.

**Why.** The reason this earns a slot. Drop if obvious from title.

**Done when.** Checkable exit state. The single condition that flips this to `done/`.

## Notes

Scratch space — blockers, decisions, links. Prune as you go.
